<?xml version="1.0" encoding="GBK"?>
<Platform>
	#foreach($platform in $platforms)
		#if($platform.eventServerIp)
			<EventServer IP="$!platform.eventServerIp" Port="$!platform.eventServerPort"/>
		#end
	#end
</Platform>
